interface HeaderProps {
  onContactClick: () => void
  onLogoClick: () => void
}

/** Bandeau global retiré : le logo « Retour à l'accueil » n'est plus affiché ici. */
const Header = (_props: HeaderProps) => {
  return null
}

export default Header
